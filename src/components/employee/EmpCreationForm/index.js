import React, { Component } from 'react'
import { connect } from 'react-redux'

class EmpCreationForm extends Component {
    render() {
        return (
            <div className={`section container col s${12}`}>
                <h2>test</h2>
            </div>
        )
    }
}


export default connect(null, {})(EmpCreationForm);